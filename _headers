# Cloudflare Pages 响应头配置
#
# 本文件必须放在「构建输出根目录」，本站无构建步骤，所以就是仓库根目录。
#
# 背景：Pages 对 .html 默认不缓存（max-age=0, must-revalidate），
#       但对 .css / .js 这类静态资源会给数小时级的浏览器缓存。
#       本站文件名不带内容 hash，于是改样式或脚本后，用户会长时间看到旧版本
#       —— clsf-viewer 里手工加的 `style.css?v=5` 就是被这个问题逼出来的。
#
# 这里统一改成「可以缓存，但每次都要回源校验」：
#   内容没变 → 服务器回 304，几乎不耗流量；
#   内容变了 → 立刻拿到新版本，不需要用户硬刷新，也不需要手工维护 ?v=N。

/*
  Cache-Control: no-cache
